<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>List</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link href="https://fonts.googleapis.com/css?family=Coiny" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Quicksand" rel="stylesheet">
    <style>
        .titul {
            font-family: 'Coiny', cursive;
        }

        .cont-tab {
            font-family: 'Quicksand', sans-serif;
        }
        input[type=submit] {
            background-color: #4CAF50;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: black;
            cursor: pointer;
            width: 10%;
            float: right;
            text-align: center;
        }
        textarea {
            width:100%;
            height: 80%;
	        padding:10px 10px 200px;
        }
        tex
    </style>
</head>

<body>
    <h1>Usuario: <%= usuario.email %></h1>
    <div class="w3-container cont-tab">
        <table class="w3-table w3-striped">
            <form action='/profile' method="POST">
                <input  type="submit" value="Profile">
            </form>
            <form action='/logout' method="GET">
                <input  type="submit" value="Cerrar sesión">
            </form>
                <p>
                    <table class="w3-table w3-border w3-bordered w3-centered">
                        <% for (let i of producto){ %>
                        <tr>
                            <td>
                                <div class="w3-card-4 w3-hover-opacity w3-hover-blue">
                                    <div class="w3-container">
                                        <h4><b><%= i.name %></b></h4>
                                    </div>
                                </div>
                            </td>
                        </tr>
                        <% } %>
                    </table>
                    <textarea id="productos" type="textarea" style="padding-left:10px" name="Productos"><%= usuario.productos %></textarea>
                </p>
        </table>
    </div>
</body>

</html>